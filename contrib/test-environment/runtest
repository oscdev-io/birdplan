#!/bin/bash

pacman -Q bird
pacman -Q exabgp

cd /root/birdplan

ARGS=("$@")
if [ ${#ARGS[@]} -eq 0 ]; then
    cpus=$(nprocs)
    jobcount=$((cpus / 2))
    ARGS=(--numprocesses=$jobcount --dist=loadfile)
fi

echo "RUNNING: tox -- ${ARGS[@]}"
nice -n 20 tox -- ${ARGS[@]}
